#include <iostream>
using namespace std;
int factorial( );
int main( )
{
	factorial( );
	 return 0;	
}
int factorial( )
{
	int n,f;
	cout<<"enter a number"<<endl;
	cin>>n;
	if (n==1)
	return 
	else
	return f=n*factorial(n-1);
	cout<<"result"<<f<<endl;
}